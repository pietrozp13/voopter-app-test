import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  dayTagsContainer: {
    flexDirection: 'row',
    margin: 2,
    borderRadius: 16,
    height: 32,
    width: 72,
    justifyContent: 'center',
    alignItems: 'center',
  },
  dayTagsText: {
    fontSize: 16,
    fontWeight: 600,
    color: '#fff',
  },
});
